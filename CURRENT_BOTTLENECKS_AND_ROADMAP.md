# 八视角 6D Pose：当前瓶颈与验证顺序

当前正式数据是 `/data_ft_9_10/wentai/projects/data/1` 的 8 个同步视角、206 个有效帧
（`000040`–`000245`）。部件首次出现帧为 `body=40`、`inner_pot=65`、`lid=89`。

## 当前主要风险

1. **点云并非稳定真值。** mask 边界、遮挡者 depth、逐帧 depth scale/offset 和相机
   外参误差都会进入融合点云。ICP 只能拟合输入点，不能修复系统性深度或标定误差。
2. **对称结构存在旋转多解。** body 和 inner pot 的轴向旋转仅靠几何点云通常不可观；
   lid 还可能出现法向翻转。单帧 ICP fitness 很好也不能证明语义朝向正确。
3. **局部可见点云会改变质心和最优配准。** 手部遮挡、出画及装配遮挡会使相邻帧看到
   不同表面，直接逐帧 ICP 容易跳变。
4. **二维高 IoU 不能单独验证完整 6D pose。** 对称物体的错误 yaw、薄盖翻面或深度方向
   偏差，可能仍得到相近轮廓。

## 当前算法约束

- `common/pose_tracking.py` 统一几何配准、平移跟踪和 mask bbox 平移策略；
- 对称轴旋转通过上一帧连续性消歧，不把不可观的 yaw 当成新的测量；
- `common/appearance_pose.py` 用多视角轮廓和 RGB 边缘在几何候选中选择方向；
- 装配静止区间和逐帧运动上限由 `configs/pose_data_1_8view.json` 约束；
- `common/pose_validation.py` 在写出前检查刚体矩阵、派生字段和运动跳变。

这些约束能阻止明显翻转和跳帧，但不能替代相机、深度、mesh 与语义轴标定。

## 验证优先级

1. 用静止 body 做逐视角 depth/外参残差审计，先排除点云系统误差；
2. 检查 Qwen→SAM track 的面积突变、手部粘连和跨视角矛盾；
3. 为每个 mesh 定义顶部法向和正前方语义轴，显式列出允许的对称群；
4. 在无遮挡关键帧用多视角 silhouette、RGB edge 和 depth 同时验收锚点；
5. 对动态段检查角速度、平移速度、法向连续性和装配区间相对 pose；
6. 最后再调 GICP voxel、correspondence distance 和鲁棒核参数。

如果没有关键帧 3D pose、语义关键点或已知尺寸，只能报告多视角重投影残差与时序
一致性，不能声称达到某个毫米或角度级绝对精度。
